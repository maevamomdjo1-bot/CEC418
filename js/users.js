// Users page — vendor sees customer orders summary
document.addEventListener('DOMContentLoaded', async () => {
  if (!requireAuth('vendor')) return;
  // Static display — no user management API needed for MVP
});
